export const RefineStep = () => {
    return (
        <div>
            We need a window on the left, and a window on the right. And a button that says "Refine". That will call chatGPT and it will ask for a better
            description.
        </div>
    );
};
