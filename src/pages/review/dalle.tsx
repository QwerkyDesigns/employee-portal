import Layout from "@/components/Layout";

export default function ReviewDallePage() {
    // TODO: useQuery to get the un-transferred files from the dalle subirectory in the creation bucket
    return (
        <Layout pageName="Review Dalle generated images">
            <div>The bit</div>
        </Layout>
    );
}
